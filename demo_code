def T_ACC(T_i , ACC_i):
    ''' 
    传入参数形如　shape : [1,个数]
    T_i = np.array([[0.2 , 0.4 , 0.8 , 0.7 , 0.001]])
    ACC_i = np.array( [[0.5 , 0.8 , 0.4 , 0.4 , 0.1]] )

    '''
    return (T_i * ACC_i) / ACC_i.sum()
